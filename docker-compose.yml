version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: secure-multi-tenant-auth-web
    ports:
      - "5178:3000"
    env_file:
      - .env.local
    restart: always
